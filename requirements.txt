# Legacy dependency note
#
# Do not install this file for the continuation baseline.
# It is intentionally kept as a marker because the original project used
# `requirements.txt` as a broad, full-feature install entrypoint, but that file
# pinned an old faster-whisper/Torch/CUDA-era stack that no longer matches the
# verified ordinary transcription path.
#
# Use these files instead:
# - requirements-dev-faster-whisper.txt for ordinary faster-whisper transcription
# - requirements-dev-cuda-cu124.txt for the optional CUDA 12.4 Torch runtime
# - requirements-dev-test.txt for repository tests
#
# A future full-feature WhisperX/Demucs dependency set should be rebuilt in a
# separate requirements file after those features are verified.
